1. The Application can be started using the "debug" argument to be launched in debug mode, that means that the programmer can navigate the app without having to
	insert a correct username and password, as a default username and password already present in the database accounts table, so that the application can be debugged easliy
	without having to insert a username and password each time to navigate the app.

2. The UML diagram is located in the root folder as a png file, but also available in the project's tree as a .puml object (Test.puml)
